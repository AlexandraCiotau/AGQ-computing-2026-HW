# AGQ-computing-2026-HW

## How to sync parent repo

In a new terminal, do the following:

```bash
cd git/AGQ-computing-2026
git pull
```

Now you should be able to see the new week in that folder.

# commits
git add .
git commit -m "partial implementation" 
git push 
